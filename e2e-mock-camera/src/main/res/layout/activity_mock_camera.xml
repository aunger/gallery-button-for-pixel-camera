<?xml version="1.0" encoding="utf-8"?>
<!--
    Solid #00C853 (GREEN) full-bleed View.
    Alternative 1 from the E2E plan: render green directly instead of a camera preview,
    because the emulator's virtual-scene camera renderer is incompatible with
    -gpu swiftshader_indirect used in CI.
-->
<View xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#00C853" />
